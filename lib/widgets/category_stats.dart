import 'package:flutter/material.dart';

class CategoryStats extends StatelessWidget {
  const CategoryStats({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('分类统计'));
  }
}
